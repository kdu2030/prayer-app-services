{
    "ConnectionStrings": {
        "DefaultConnection": "Use environment variable CONNECTION_STRINGS__DEFAULTCONNECTION"
    },
    "Jwt": {
        "Key": "Use environment variable JWT__KEY",
        // FIXME: This needs to point to the deployed URl
        "Issuer": "http://localhost:5000",
        "Audience": "http://localhost:5000"
    },
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    }
}
